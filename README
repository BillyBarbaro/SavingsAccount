This project was designed for Ubuntu 12.04.5 LTS.
It is compiled using gcc
No extra libraries or special paths are needed

It should be noted that the program assumes that over the course of
transactions, enough money will be deposited to service all the withdrawers.
Otherwise, the last one would wait indefinitely for another deposit.

The program can be compiled using the command 'make'.  The directory cleaned
using 'make clean'.  Running the program is done by calling 'make run'.
